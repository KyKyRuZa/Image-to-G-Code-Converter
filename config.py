DEFAULT_CONFIG = {
    # === ОБЩИЕ ПАРАМЕТРЫ ===
    "scale_factor": 0.8,          # Масштабный коэффициент для изображения
    "preview_scale": 0.8,         # Масштаб предпросмотра в интерфейсе
    "safety_margin": 5.0,         # Минимальная безопасная высота подъема (мм)
    "max_z_height": 50.0,         # Максимальная высота по оси Z (мм)

    # === ПАРАМЕТРЫ ОБРАБОТКИ ИЗОБРАЖЕНИЯ ===
    "canny_min": 50,              # Минимальный порог для детектора Кэнни
    "canny_max": 150,             # Максимальный порог для детектора Кэнни
    "min_contour_length": 30,     # Минимальная длина контура (в пикселях)
    "draw_contours": True,        # Рисовать контуры изображения
    "morph_kernel_size": 3,
    "morph_iterations": 1,
    "brightness_threshold": 0.6,
    "max_line_gap": 5,
    "smoothing_algorithm": "none",
    "smoothing_intensity": 3,
    "max_scale_factor": 0.9,      # МАКС масштаб
    "min_scale_factor": 0.1,      # МИН маштаб
    "margin_factor": 0.98,        # Отсупы

    # === ПАРАМЕТРЫ ШТРИХОВКИ ===
    "hatch_density": 12,          # Плотность штриховки (количество линий)
    "hatch_angle": 45,            # Угол штриховки в градусах
    "draw_hatching": True,        # Рисовать штриховку
    "hatch_cross": True,          # Использовать перекрестную штриховку
    "min_line_length": 5,         # Минимальная длина линии штриховки (в пикселях)
    "hatch_depth": -1,            # Глубина штриховки (для 3D принтеров, мм)
    
    # === ПАРАМЕТРЫ СТАНКА ===
    "work_area_x": 200.0,         # Рабочая область по X (мм)
    "work_area_y": 200.0,         # Рабочая область по Y (мм)
    "pen_up_z": 5.0,              # Высота Z при поднятом пере/маркере (мм)
    "pen_down_z": -0.5,           # Высота Z при опущенном пере/маркере (мм)
    "feed_rate": 800,             # Скорость подачи при рисовании (мм/мин)
    "rapid_feed_rate": 2000,      # Скорость быстрого перемещения (мм/мин)
    "optimize_order": True,       # Оптимизировать порядок рисования
    
    # === ПАРАМЕТРЫ ШРИФТА ===
    "font_scale": 2,              # Масштаб шрифта Hershey
    "font_spacing": 1.2,          # Межсимвольный интервал
    "font_line_height": 1.5,      # Межстрочный интервал
    "text_position_x": 0.5,       # Относительная позиция X (0-1)
    "text_position_y": 0.9,       # Относительная позиция Y (0-1)
    "text_align": "center",       # Выравнивание текста: left, center, right
    "text_max_width": None,       # Максимальная ширина текста (None = без ограничений)
    
}